# 6.1.4 L2 Cache

@{Key=6.1.4; Title=L2 Cache; Body=All GPU units communicate to memory through the Level-2 (L2) Cache, also known as the L2. The L2 Cache sits between on-chip memory clients and the framebuffer. It is connected to the XBAR (to communicate with GPC units), the CROP, and ZROP units. In addition to providing the caching functionality, the L2 also includes hardware to perform compression and global atomics.}(@{Key=6.1.4; Title=L2 Cache; Body=All GPU units communicate to memory through the Level-2 (L2) Cache, also known as the L2. The L2 Cache sits between on-chip memory clients and the framebuffer. It is connected to the XBAR (to communicate with GPC units), the CROP, and ZROP units. In addition to providing the caching functionality, the L2 also includes hardware to perform compression and global atomics.}.Body)
