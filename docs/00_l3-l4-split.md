# 00 (L3/L4 Split)

- The DMA writes the Ethernet header + IP header +
- TCP or UDP header into the
header buffer.

10G Ethernet Controller Functional Description
- Packet Type
- SPLM
- Description
- IP Packet (Not TCP/UDP)
- The DMA writes the Ethernet header + IP header
into the header buffer.
- Non-IP Packet
- The DMA does not split the header and payload
- Any Packet

