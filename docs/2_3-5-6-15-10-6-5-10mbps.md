# 2 ,3,5,6 15,10,6,5 10Mbps

- 60/10 = 6

- Controller Area Network (CAN)
S.No.
- Data rate
- BTP x nTQ
- Valid BTP
- Valid nTQ
15Mbps =60/15 = 4
##### 10.9.2.1.2 FSI CAN Clocking
For FSI CAN controller instances, the CCLK and HCLK are both fixed at 40 MHz. For a Full-Duplex (FD) operation, a CAN clock frequency of 20 MHz, 40 MHz, or 80 MHz is recommended to guarantee interoperability between different CAN FD implementations. A 40 MHz CCLK is sufficient to provide CAN bit rates up to 10 Mbps.
- Refer to the data sheet for the Orin product being used to determine the maximum data
rates supported. Not all rates shown here are applicable to all products.
#### 10.9.2.2 CAN Resets
Each CAN controller gets a signal reset from SoC. This reset is sync'ed to CAN clocks before it drives logic in a particular CAN Clock domain. Reset for PCLK domain logic is sync'ed to PCLK.
#### 10.9.2.3 External Time Stamp
The external time stamp is required in the CAN FD mode. The internal time stamp is not used in the CAN FD mode with the bit-rate switch. A 16-bit port is available from the M_TT_CAN to hook up the external time stamp counter. The TSC terminal node is available in the AON cluster PG partition. As per the driver requirements, a 1 µs to 10 µs granularity is required for time stamping. FSI_XTAL clock operates at 40 MHz. The terminal node operates under the TSC reference clock which is 31.25 MHz. With this frequency, the terminal node counter LSB gives a 32 ns granularity. By considering tsc [20:5], the result is a 1.024 µs granularity with a wraparound of 67 ms. Since the granularity requirement varies, a way to select the required offset is needed. A 5-bit MUX select is provided which selects the different offsets of the TSC counter which gives the required granularity: granularity = 1.024 µs * 2 ** N Where N = 0, 1, 2..15
- The OFFSET_SEL field in the M_TTCAN_TIME_STAMP_0 register is used to select the required
offset. Note that the value should not be changed dynamically when CAN is in operation.

- Controller Area Network (CAN)
The micro-architecture diagram of the time stamp hook-up is shown below. One deep async FIFO is used to synchronize the counter from TSC reference clock to the CAN HCLK. As described above, the starting granularity is 1.024 µs, therefore, there is a new counter value in the offset tsc[20:5] for once in every 32 tsc_ref_clk cycles. The new value is reflected in tsc[20:5] when tsc[4:0] is zero.
- This is the write condition for async FIFO. The time stamp selection bit
m_ttcan_time_stamp_0_timer_sel is provided between the programmable register and TSC counter.
**Figure 10.139 Time Stamp Hook Up**
The time stamp wraparound interrupt is generated even for the external time stamp configuration. For external time stamp mode: 1. 2. 3. 4. 5. TSCV register accurately reflects the external time input. Rx time stamps (i.e., RXTS) reflects the external time input for normal and FD modes. Tx event timestamps (i.e., TXTS) reflects the external time input for normal and FD modes. Timeout counter (TOCC) works correctly in FD mode. Time stamp wraparound is generated.

- Controller Area Network (CAN)
#### 10.9.2.4 CAN Message RAM
- Each CAN controller has a separate 4KB Message RAM to store incoming/outgoing Messages, TX
Messages timestamp, and Filters to be applied on incoming Messages.
- The generic master interface and CAN APB slave interface are both synchronous to
aon_apb_clk. The Message RAM Wrapper module can pipeline accesses to Message
- RAM. Message RAM is parity protected at byte boundaries to comply with safety
requirements.
#### 10.9.2.5 Round Robin Arbiter Between APB and CAN Core
- Since both APB and CAN Core can access the Message RAM. An arbiter is required between the
write/read requests coming from the two masters. The arbiter used is a simple round-robin arbiter. A few points about the arbiter: It takes the arbiter three HCLKs to service a request.
- When both masters place the request at the same time, the grant is given to the master who
didn't get grant the last time When a master requests access while a request is already in process, the request is stalled until the ongoing request is completed.
- The arbitration happens between requests of the same kind only (i.e., between reads or
between writes and never between a read and a write). In case of read request from one master and write request from another master, write request is given preference.
#### 10.9.2.6 Configuration and Status Registers
There are three auxiliary interfaces provided by TT_CAN Core, these interfaces aggregate signals required for Clock calibration unit, Debug Message unit, coupling two Time-Triggered networks, and some other status signals. These signals are provided as a register interface in case software wants to use them.
- Table 10.68 Status Signals
- Signal Name
- Direction
- Clock Domain
- Comment
dis_mord
- IN
- HCLK
- Required for Production testing. Software
programmable

- Controller Area Network (CAN)
- Signal Name
- Direction
- Clock Domain
- Comment
- Swt
- IN
- ASYNC
- Required for Synchronization of two TT CAN
- Networks. This is the software override for SWT
signal, it can be otherwise driven from SOC, TMP, or RTP signals coming from another CAN controller
- Evt
- IN
- ASYNC
- Required for Synchronization of two TT CAN
- Networks. This is the software override for EVT
signal, it can be otherwise driven from SOC, TMP, or RTP signals coming from another CAN controller
- Soc
- OUT
- HCLK
- Required for Synchronization of two TT CAN
- Networks. This signal is also provided as the
output of CAN controller so that it can be used by another CAN controller to drive its SWT and
- EVT pins
- Tmp
- OUT
- CCLK
- Required for Synchronization of two TT CAN
- Networks. This signal is also provided as the
output of CAN controller so that it can be used by another CAN controller to drive its SWT and
- EVT pins
- Rtp
- OUT
- CCLK
- Required for Synchronization of two TT CAN
- Networks. This signal is also provided as the
output of CAN controller so that it can be used by another CAN controller to drive its SWT and
- EVT pins
- Ascm
- OUT
- HCLK
- Required for Asynchronous Serial
- Communication. ASC not implemented
- Asct
- OUT
- HCLK
- Required for Asynchronous Serial
- Communication. ASC not implemented
dma_ack
- IN
- HCLK
- Required for DMA Message Unit
dma_req
- OUT
- HCLK
- Required for DMA Message Unit
- Cok
- IN
- HCLK
- Required for Clock Calibration Unit. CCU not
implemented ir[31:0]
- OUT
- HCLK
- Though this bus is not used for driving interrupt
controllers it is used for generating faults to HSM. ttir[18:0]
- OUT
- HCLK
- Though this bus is not used for driving interrupt
controllers it is used for generating faults to HSM. txbrp[31:0]
- OUT
- HCLK
Not required.
- Rxfd
- OUT
- CCLK
Not required.
- Txfd
- OUT
- CCLK
Not required. fe[2:0]
- OUT
- HCLK
- Required for Clock Calibration Unit. CCU not
implemented

- Controller Area Network (CAN)
- Signal Name
- Direction
- Clock Domain
- Comment
- Cce
- OUT
- HCLK
- Required for Clock Calibration Unit. CCU not
implemented
- Spt
- OUT
- CCLK
- Required for Clock Calibration Unit. CCU not
implemented
- Mrx
- OUT
- CCLK
- Required for Clock Calibration Unit. CCU not
implemented
- Calf
- OUT
- CCLK
- Required for Clock Calibration Unit. CCU not
implemented
- Aff
- OUT
- HCLK
- Required for Clock Calibration Unit. CCU not
implemented
#### 10.9.2.7 Time-Triggered (TT) CAN
##### 10.9.2.7.1 TT Related Features Supported
- TTCAN Level 0, 1, and 2
- Time Mark Interrupts
- Stop Watch
- Watchdog Timer
- Synchronization to external events
##### 10.9.2.7.2 TT Interface Between the Two CAN Controllers
In CAN based networks (with two CAN controllers and each CAN controller expected to sit on two different Networks), a node such as an NVIDIA SoC can act as a potential gateway between the two networks. In addition to forwarding messages between the two networks, in TT Networks a gateway can compare and align cycle time, global time, and even local time of the two networks (e.g., a Time Master of both networks resided inside the NVIDIA SoC and needed to sync with the other network). To enable this functionality m_ttcan_rtp (RTP – Register TM Interrupt Pulse), m_ttcan_tmp (TMP – Trigger TM Interrupt Pulse), and m_ttcan_soc (SoC – Start of Cycle) outputs of one CAN controller are connected to another CAN controller. These signals are connected to m_ttcan_evt and m_ttcan_swt inputs. For both m_ttcan_evt (EVT – Event Trigger) and m_ttcan_swt (SWT – Stop Watch Trigger) there are four connectivity options – m_ttcan_rtp, m_ttcan_tmp, and m_ttcan_soc outputs from the other CAN controller and a configuration and status register (CSR) field based signal from the same CAN controller.

- Controller Area Network (CAN)
#### 10.9.2.8 Bosch IP Specific Features
##### 10.9.2.8.1 Asynchronous Serial Communication
This is a feature supported by the Bosch module but not supported in Orin.
#### 10.9.2.9 Notable Bosch IP Behavior
##### 10.9.2.9.1 Arbitration Between STD Message and EXT Message
Per the CAN 2.0 specification, EXT ID has two parts: Base ID and Extended ID. If Base ID of Extended ID Message is lower in priority (has a higher numerical value) than STD ID Message then STD ID Message WINS arbitration.
- If Base ID of Extended ID Message has same priority (has same numerical value) as STD ID
Message then STD ID Message WINS arbitration. If Base ID of Extended ID Message is higher in priority (has lower numerical value) than STD ID Message then EXT ID Message WINS arbitration.
- Further arbitration between EXT ID Messages with same Base ID is done based on Extended ID
part.
##### 10.9.2.9.2 Prefetch Feature on Transmit Message Handler
The Transmit Message Handler has a prefetch buffer of two, so it can retrieve two Messages from the CAN Message RAM, for which the request is pending, even if the controller is in INIT state. This may lead to unexpected ordering in transmission.
##### 10.9.2.9.3 Debug Message Interface
- Bosch's CAN controller provides a Debug Message sequence. Bosch's CAN IP provides a DMA
(Debug Message) Interface to receive some diagnostic messages from the CAN Interface. This interface is not exposed in hardware. It is tied to some Config and Status registers. If software needs to use the DMA interface, the following sequence is to be implemented: 1.
- Observe DMA_REQ (There is no interrupt fired for DMA_REQ, so we need to poll for
- DMA_REQ)
- Read DMA Packets from CAN Message RAM
- Assert DMA_ACK
- Wait for DMA_REQ to go low
2. 3. 4.

- Controller Area Network (CAN)
- Deassert DMA_ACK
5. DMA_REQ and DMA_ACK are register bits in CAN register map.
##### 10.9.2.9.4 Low Power Modes
Bosch's CAN controller supports a clock stop signal and the Host asserts this signal to the CAN Controller whenever it wants to shutdown the CAN clock. The CAN controller in turn drives an acknowledge signal when all outstanding commands are completed.
#### 10.9.2.10 System Interfaces
CAN uses the standard APB interface to connect with the Control Fabric. CAN Rx and CAN Tx signals connect with the CAN PHY outside of the chip. Error pulse for message RAM parity error indication to AON/FSIError collator in the respective cluster. Fault signal for connectivity with HSM via AON/FSIError collator in the respective cluster. Input TSC for Timestamping of CAN messages during CAN data transfer. Two Interrupts from the CAN controller to connect with AON VIC and LIC in the AON cluster.
- AON-CAN Interrupts are also routed to the AON-GTE module. The GTE module is used to
timestamp various CAN events that can result in an interrupt. FSI-CAN interrupts (two per instance) are connected to the FSI_GIC and CSHM_AVIC.
#### 10.9.2.11 CAN SoC Interface
Each CAN Interface has six pins:
- CAN_TX
- CAN_RX
- CAN_STB
- CAN_WAK
- CAN_EN
- CAN_ERR
1. 2. 3. 4. 5. 6. These pins are driven from pads that have a switchable voltage supply (between 1.8V and 3.3V). Only CAN_TX and CAN_RX interface with the CAN Controller. Others are GPIOs controlled directly by CAN software.

- Controller Area Network (CAN)

