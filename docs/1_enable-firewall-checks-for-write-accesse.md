# 1 = Enable firewall checks for write accesses through the firewall. 0 = Disable firewall checks for write accesses through the firewall. 0 = DISABLE 1 = ENABLE 0x0 REN:

- Read firewall check enable

